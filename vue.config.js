module.export = {
    lintOnSave: false, //配置eslint，不生效
    devServer: {
        open: true,
        overlay: {
            warning: false, //配置eslint，不生效
            errors: false //配置eslint，不生效
        }
    },
}
